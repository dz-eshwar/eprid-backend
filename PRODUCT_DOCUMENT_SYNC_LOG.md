# PRODUCT_DOCUMENT.md sync log

Append-only. Each entry records the HEAD commit synced to in each repo, what changed in PRODUCT_DOCUMENT.md, and review status.

## 2026-07-06 (first run)

- No prior log existed — used `git log --since="30 days ago" --stat` in both repos as the starting window (both repos' entire history falls inside 30 days: backend created 2026-06-30, frontend created 2026-06-26).
- Synced backend HEAD: `7a95b2e235140200e9849065eb232fd69178dfc8`
- Synced frontend HEAD: `96544685fcf156ac358b9d8d35921154574e261c`
- Commits reviewed:
  - Backend: `d0b3828` (initial setup, 90 files — already reflected in existing doc content), `44c7fd4`/`c0590f3`/`f72206c`/`1f279ef`/`129d0bd` (CORS allowed-origins bug fixes for production, deployment-only, no doc-visible behavior change), `7a95b2e` (fixed NPE / null-field bug on GET /checks, regulatory-history, report, evidence via lazy-loading JOIN FETCH — internal correctness fix, doesn't change any documented route or behavior, no doc change made).
  - Frontend: `1590d7d` (create-next-app init), `a612505` (regulatory history component, verify flow, dashboard — already reflected), `d752f76` (landing page + Resend waitlist), `9da627c` (JSX fix), `7e16798`/`b0fb03d` (brand mark + navbar contrast fix), `5d214b0` (next-intl i18n added, routes moved under `app/[locale]/`), `3a42748` (locale switcher fix), `b7b025f` (waitlist replaced with direct `/register` CTA), `9654468` (PUBLISHER role-gating on verify form, report download fix).
- Changes made to PRODUCT_DOCUMENT.md:
  - §2 Architecture: added note that frontend routes are locale-prefixed (`/[locale]/...`, en/hi/te via next-intl, `proxy.ts` middleware) and that cited paths omit the prefix.
  - §3: added new "Landing page" entry (public marketing site, direct-signup CTA, dead `/api/waitlist` route left over from removed waitlist flow).
  - §3 Module A: noted `RecyclerDetailsForm` role-gating (PUBLISHER vs CONSULTANT/others) and the report-download JWT-fetch fix.
  - §4 Data model: added `PUBLISHER` to `User` role list (was already in code since the initial commit, just missing from the doc).
- Edit is uncommitted in the backend repo working tree, waiting for Eshwar's review.

## 2026-07-06 (second run)

- No changes since last sync — both repos' HEAD are identical to the last-synced hashes.
- Backend HEAD unchanged: `7a95b2e235140200e9849065eb232fd69178dfc8`
- Frontend HEAD unchanged: `96544685fcf156ac358b9d8d35921154574e261c`
- No commits to review; PRODUCT_DOCUMENT.md not touched.
- Note (observation only, not a doc change): both repos have substantial uncommitted working-tree changes not yet reflected in any commit — backend touches SecurityConfig, EvidenceController, RecyclerController, RegulatoryHistoryController, VerificationCheckController, auth/check/recycler DTOs, and Producer/Recycler/VerificationCheck entities; frontend touches dashboard page, AuthForm, LandingPage, Navbar, RecyclerDetailsForm, api types, and all three locale message files, plus a new untracked `app/[locale]/used-oil/` directory. Per task scope this run only tracks committed history, so none of this is reflected above — will pick it up once committed.

## 2026-07-07 (third run)

- Synced backend HEAD: `b36e5bbf8087724b2391b11e62951d63b4a835f9`
- Synced frontend HEAD: `456bca21b16485c7346cb1749d414064add39ecf`
- Commits reviewed:
  - Backend: `b36e5bb` — "feat: add used oil registration module with tier determination, fee calculation, and PDF generation." This single commit contains the full Module A0 (KYC gate)/Module D (tyre)/Module E (used-oil) build plus `PlausibilityStrategy` refactor, migrations V8/V9, and — notably — it also swept up PRODUCT_DOCUMENT.md and PRODUCT_DOCUMENT_SYNC_LOG.md as committed files for the first time (150 and 26 lines respectively), meaning the doc content from this task's first two runs is now committed, not just sitting in the working tree. Checked the doc against the actual diff: already accurate, no correction needed for the code this commit adds.
  - Frontend: `8b6e1b7` (Used-Oil page, CredentialChecksCard, UsedOilAssistantForm/SummaryResult, waste-stream selector, landing page waste-streams promo section + nav links, RegisterForm KYC fields — all already reflected in the doc except the landing/nav additions below), `9bc0b49` (translation-only: waste-stream fields added to recycler-details-form strings in en/hi/te, no doc-visible change), `456bca2` (empty commit, redeploy trigger only, no file changes).
- Changes made to PRODUCT_DOCUMENT.md:
  - §3 Landing page: added mention of the new `StreamsSection` (3-card Battery/Tyre/Used-Oil promo, added when Module E shipped) and the new direct `/used-oil` link added to both the public landing nav and the authenticated app `Navbar`. Everything else in this commit (RegisterForm KYC fields, CredentialChecksCard on dashboard, RecyclerDetailsForm waste-stream selector, UsedOilAssistantForm/SummaryResult) was already correctly documented from prior runs.
- Note (observation only, not a doc change): the same uncommitted working-tree changes flagged in the second-run entry above are still uncommitted in the backend repo (SecurityConfig, EvidenceController, RecyclerController, RegulatoryHistoryController, VerificationCheckController, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities) — unchanged since last run, still awaiting a commit before this task can review them.
- Edit is uncommitted in the backend repo working tree, waiting for Eshwar's review.

## 2026-07-07 (fourth run)

- Synced backend HEAD: `3b02fa8baecb180e869f2c67ad1613c480e367ea`
- Synced frontend HEAD: `c949c40c7779c3a22a90d03dbff7d3520280775e`
- Commits reviewed:
  - Backend: `b674b96` — "feat: implement tyre EPR credit reconciliation logic and update related models." Replaces the earlier 400–450 L/tonne TPO-yield-ratio benchmark (`TyreBenchmarks`, deleted) with CPCB's own certificate-generation formula, QEPR = QP × CF × WP (`TyreEndProduct` enum + `TyreEprReconciliation`, migration V10 adds `tyre_end_product`/`tyre_imported`/`claimed_epr_credit_kg` to `verification_checks`). This commit's own diff also happened to carry the previous (third) run's uncommitted PRODUCT_DOCUMENT.md/SYNC_LOG.md edits into the repo's history — noted for context, not a code change to document.
  - Backend: `3b02fa8` — "feat: implement composite risk scoring and hard-disqualification logic for verification checks." New `CompositeScoringService` + `CompositeScoreWeights` (PRD §7.1a): five weighted sub-scores (registration, capacity, invoice, forensics, regulatory), recomputed at check creation / evidence upload / regulatory-history completion, mapped to LOW/MEDIUM/HIGH/CRITICAL (new `RiskRating.CRITICAL`). Hard-disqualification (capacity ratio >3x or active NGT/suspension finding) forces score=100. Migration V11 adds the score columns to `verification_checks`. Also added `GET /api/v1/checks/{id}/evidence` (read-later evidence summary endpoint) and a PDF-text sanitizer in `ReportService` (WinAnsi encoding, was throwing on curly quotes/em-dashes from LLM regulatory summaries).
  - Frontend: `330ee6f` (RecyclerDetailsForm tyre fields — end-product dropdown, EPR credit input, imported checkbox — matching the backend's new tyre DTO fields; translations added to all 3 locales), `c949c40` (new `/checks` list page and `/verify/[checkId]` detail page, both rendering a new shared `CheckResults` component; new `CompositeScoreCard` and shared `RiskPill` — extracted from dashboard, gained a CRITICAL style; `VerifyFlow`'s inline results step now delegates to `CheckResults` instead of duplicating the layout; dashboard links to `/checks` instead of `/verify` and rows now navigate to the detail page).
- Changes made to PRODUCT_DOCUMENT.md:
  - §3 Module A: rewrote intro to mention composite scoring + CRITICAL rating; added a full new paragraph on `CompositeScoringService` (recompute timing, 5 sub-scores, neutral-50 default, uncalibrated weights caveat, hard-disqualification and which of the PRD's rules are/aren't wired); noted the PDF report now includes the score breakdown and sanitizes text; added the new `GET .../evidence` route; rewrote the frontend bullet for `/checks`, `/verify/{checkId}`, shared `CheckResults`/`CompositeScoreCard`/`RiskPill`.
  - §3 Module D: replaced the stale TPO-yield-benchmark description with the QEPR=QP×CF×WP reconciliation logic, end-product enum, and the imported-tyre WP override; noted the old benchmark file was deleted.
  - §4 Data model: added the new tyre fields (`tyreEndProduct`, `tyreImported`, `claimedEprCreditKg`) and the composite-scoring fields on `VerificationCheck`; noted `RiskRating` gained `CRITICAL`.
  - §8 Known gaps: updated the Module D gap (yield benchmark now CPCB-sourced, but deviation tolerances still provisional); added two new gaps for composite-score calibration and partial hard-disqualification-rule coverage.
- Note (observation only, not a doc change): the same uncommitted backend working-tree changes flagged in runs two and three are still present and largely unchanged in scope (SecurityConfig, EvidenceController, RecyclerController, RegulatoryHistoryController, VerificationCheckController, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, plus gradle wrapper files and `application.yml` this run) — still uncommitted, still awaiting a commit before this task can review them. Frontend working tree is currently clean (only an untracked `.claude/settings.local.json`, not reviewed — tooling config, not product code).
- Edit is uncommitted in the backend repo working tree, waiting for Eshwar's review.

## 2026-07-08 (fifth run)

- Synced backend HEAD: `d01948b254ee869a47048be00df0c71ca35cc0fe`
- Synced frontend HEAD: `f16eae1bf074fbc928ce0615c77d065d873684b7`
- Commits reviewed:
  - Backend: `d01948b` — "feat: add CPCB recycler directory with ingestion, scoring, and search functionality." New standalone module: `CpcbRecycler`/`CpcbRecyclerAuthorization`/`CpcbRecyclerScore`/`CpcbGeoRiskHotspot` entities (migrations V12 + V13 seed), CSV ingestion (`CpcbRecyclerCsvParser`, Apache Commons CSV added to `build.gradle.kts`) with a partial-capture heuristic, `CpcbRecyclerScoring` (Entity Health Score — registration/authorization/geography only, explicitly not a Certificate Risk Score since this data source has no certificate-volume/invoice fields), `CpcbRecyclerSearchService` (excludes individual-contact PII from responses), `CpcbRecyclerController` (`POST .../ingest` ADMIN-only, `GET .../search` any authenticated role). This commit's own diff also carried the previous (fourth) run's uncommitted PRODUCT_DOCUMENT.md/SYNC_LOG.md edits into the repo's history — noted for context, not new code to document.
  - Frontend: `f16eae1` — new `/cpcb-directory` page (sign-in gate, `CpcbRecyclerSearchForm` + `CpcbRecyclerResultCard`), `lib/api/cpcbRecyclers.ts` + new types in `lib/api/types.ts`, `Navbar` link (hidden for `RECYCLER` role), translations in all 3 locales.
- Changes made to PRODUCT_DOCUMENT.md:
  - §3: added new "Module F — CPCB Recycler Directory" section covering ingestion/upsert-by-CPCB-id/partial-capture heuristic, the Entity Health Score point values (which are spec-given vs. assumed placeholders), the geographic-hotspot check, the not-implemented ISO bonus, PII exclusion from search results, and backend/frontend routes.
  - §4 Data model: added the four new entities (`CpcbRecycler`, `CpcbRecyclerAuthorization`, `CpcbRecyclerScore`, `CpcbGeoRiskHotspot`).
  - §8 Known gaps: added a Module F bullet (Entity-Health-only scope, unverified point values/capacity units, ISO bonus not implemented, static hotspot list).
- Note (observation only, not a doc change): the same uncommitted backend working-tree changes flagged in runs two through four are still present, unchanged in scope (SecurityConfig, EvidenceController, RecyclerController, RegulatoryHistoryController, VerificationCheckController, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, `application.yml`) — still uncommitted, still awaiting a commit before this task can review them. Also new this run: an untracked `CLAUDE.md` at the backend repo root (agent/tooling config, not product code — not reviewed) and an untracked `.claude/settings.local.json` in both repos.
- Edit is uncommitted in the backend repo working tree, waiting for Eshwar's review.

## 2026-07-09 (sixth run)

- Synced backend HEAD: `ba517e44569cda0aad62056888a8322c23604875`
- Synced frontend HEAD: `13fa8f9eff765d4dc1045db60fee545180a0c0ea`
- Commits reviewed:
  - Backend: `4fdcdfd` — extends `CpcbRecycler` with a second batch of optional CPCB fields (web address, phone, install/operating dates, ISO 9001/14001 + APCM/WPCM upload flags, raw application/payment status, certificate no./date, MRAI/SOP/ESG flags) and wires the previously-stubbed ISO 9001/14001 bonus into `CpcbRecyclerScoring` (`-10` pts when both flags true, inert today — 0/553 rows populated); adds migrations V14 (columns) and V15 (full 553-row directory seed, supersedes V13's sample). This commit's own diff also carried the previous (fifth) run's doc/log edits into the repo's history — already accurate, no correction needed.
  - Backend: `2e5026c` — pure rename, `ScoreConfidence.ENTITY_HEALTH` → `ENTITY_RISK` (entity, scoring service, controller Swagger text) + migration V16 to update existing DB rows and the column default. Rationale documented in-code: "health score" reads as higher-is-better, which is backwards for a field where higher = riskier.
  - Backend: `e7513df` — `AuthService.register` now allow-lists self-registerable roles (`CONSULTANT`/`PRODUCER_STAFF`/`RECYCLER`/`PUBLISHER`); any other role, including `ADMIN`, is rejected. Closes a prior self-registration hole.
  - Backend: `ba517e4` — despite the commit message being a copy-paste of `e7513df`'s ("enforce self-registration role restrictions"), the actual diff is unrelated: adds `CpcbStateCode` entity/repository + migration V17 (raw CPCB `state_id` → inferred state name, 20 codes, sourced from address text since CPCB doesn't publish this mapping), a new `GET /api/v1/cpcb-recyclers/states` endpoint, and changes the search filter from `stateId` to `state` (a state name, resolved server-side; unresolvable name returns zero results). Search results now also carry a resolved `stateName`.
  - Frontend: `c3576ba` (Entity Health → Entity Risk terminology + a "higher score = higher risk" UI note, matching backend `2e5026c`), `4c150cc` (state search input changed from free-text to a `<select>` populated from the new `/states` endpoint, matching backend `ba517e4`), `54dcdd5` (adds an explicit risk-level `RiskPill` next to the numeric score on `CpcbRecyclerResultCard`, previously score-only).
- Changes made to PRODUCT_DOCUMENT.md:
  - §3 Module F: renamed Entity Health Score → Entity Risk Score throughout, with the rationale and UI direction-note; documented the new optional `CpcbRecycler` fields (scored-on ISO flags only); corrected the stale "ISO bonus not implemented" claim to reflect that it's wired up but inert (no populated data yet); replaced the `stateId` search-filter description with the new state-name resolution via `cpcb_state_codes` + the new `/states` route; expanded backend/frontend bullets (added `CpcbStateCodeRepository`, migrations V14–V17, the `/states` route, the state `<select>` + risk-level pill on the frontend).
  - §4 Data model: added the new `CpcbStateCode` entity row.
  - §5 Auth: added a paragraph on the self-registration role allow-list and the `ADMIN` hole it closes.
  - §8 Known gaps: updated the Module F bullet (Entity Risk Score naming, ISO bonus now wired-but-inert instead of "not implemented", added the state-code mapping's inferred/partial-coverage caveat).
- Not documented (no committed backend support yet): frontend commit `13fa8f9` (landed mid-run) adds UI copy/translations for an "e-invoice QR originality" forensics check (`lib/forensicsExplanations.ts`, all 3 locale files) describing signature verification against a government invoice portal's public key. The corresponding backend code (`EInvoiceIrp` constants, `IrpPublicKey` entity/repository, an `einvoice` service package, migration V18) exists only as **untracked** files in the backend working tree — not committed, so not documented per this task's rule against describing anything not backed by a real commit. Will pick this up once backend commits it.
- Note (observation only, not a doc change): the backend working-tree changes flagged in runs two through five are still present and still uncommitted (SecurityConfig, EvidenceController, RecyclerController, RegulatoryHistoryController, VerificationCheckController, several DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, `application.yml`, plus this run's new untracked e-invoice package noted above). Frontend working tree has uncommitted changes to `CLAUDE.md`, the CPCB-directory page/components, `lib/api/cpcbRecyclers.ts`/`types.ts`, `lib/forensicsExplanations.ts`, and all 3 locale files, plus an untracked `.claude/settings.local.json` — not reviewed, awaiting commit.
- Edit is uncommitted in the backend repo working tree, waiting for Eshwar's review.

## 2026-07-10 (seventh run — reconciliation with EPRid-validation mirror, done via Claude Cowork session with both repos + EPRid-validation folder mounted)

- Synced backend HEAD: `4a763d1998b7a5f023cdcc0a3013b5cca5bf5ee2`
- Synced frontend HEAD: `1c6f04a129d80c0ac6cae3f8b3d986a88adcadaf`
- Commits reviewed (backend, 3 since last run — the e-invoice package flagged as untracked last run is now committed):
  - `a56462e` — "implement e-invoice QR originality check with trusted IRP public keys" (this commit's own diff touched PRODUCT_DOCUMENT.md/SYNC_LOG.md but did not actually add IRN-specific documentation content — a gap in that run, backfilled now). QR extraction → JWS decode/verify against a per-IRP public key store; daily key-refresh job for NIC/Cleartax; Cygnet/IRIS marked untrusted, fail-closed (`COULD_NOT_VERIFY`, never a guessed pass). Feeds `invoiceSubScore`.
  - `bed7914` — "add battery composition checks and hard-disqualification rules for recyclers". New `BatteryCompositionCheckService` (chemistry-range check against CPCB's published table, `ClaimedMetalRecovery`/`MetalCompositionCheck` entities, `V19` migration), `RegistrationValidityAtDateService` (rule 4), `CpcbRecyclerLinkService` (`suggestMatches`/`link`, prerequisite for rules 4-5, deliberately no auto-apply given unconfirmed GST format), hard-disqualification rules 3-5 wired into `CompositeScoringService`.
  - `4a763d1` — "fix percentage calculation for metal composition checks to prevent truncation errors". Same-day bug fix: dividing before multiplying by 100 truncated small trace-metal percentages to 0.0000, silently defeating the zero-cell violation check on the exact scenario it exists to catch (found via manual testing, reported through the Cowork session). Fixed calculation order, widened `claimed_pct` column precision (`V20`), added regression test.
- Cross-referenced against `EPRid-validation/core/product_document_built_state.md` (a parallel, less formal mirror maintained across the same period in a separate Cowork session) and `EPRid-validation/core/EPRid_validation_tracker.xlsx`. Two findings from that cross-reference, not from a commit diff:
  1. **Schedule II target mismatch** (🔴 urgent, logged in §6/§8 of PRODUCT_DOCUMENT.md) — `RecoveryTargets.kt`'s hardcoded percentages don't match the tracker's primary-source gazette read. Needs resolution before Module B is trusted for a real producer.
  2. **Geographic hotspot table provenance** — confirmed (not just suspected) that `cpcb_geo_risk_hotspots` (Module F, battery Entity Risk Score) reuses the same 5 locations originally researched for the *tyre* hotspot check, including 2 rows the original PRD research downgraded to an uncorroborated general finding. A live sanity-check run (Attero/Lohum/Rubamin, done in the EPRid-validation session) found Lohum Cleantech scoring Critical partly on this table's "Haryana hotspot match" — a real, non-hypothetical instance of the caveat firing on a credible player.
- Changes made to PRODUCT_DOCUMENT.md: §3 Module A (added battery composition check + IRN Tier 1 + updated hard-disqualification rule count from 2/6 to 5/6, with the linking prerequisite spelled out); §3 Module F (added the hotspot-table provenance note); §4 Data model (added `ClaimedMetalRecovery`, `MetalCompositionCheck`, `IrpPublicKey`); §6 Regulatory content (EC rate cross-verified against the tracker — no discrepancy; Schedule II mismatch flagged urgent); §8 Known gaps (hard-disqualification count updated, IRN Tier 2/3 gap added, Schedule II mismatch added).
- Not resolved this run, deliberately: the backend working-tree diff noise flagged in runs two through six was re-checked — it's line-ending normalization (CRLF/LF), not functional changes (confirmed by diffing `SecurityConfig.kt`, every changed line is identical content). Safe to stop flagging this as an unreviewed-changes risk in future runs; it isn't one.
- Recommendation for `EPRid-validation/core/product_document_built_state.md`: that file should point to this document as the authoritative source going forward rather than duplicating it — it was a reasonable stand-in while this document was unreachable from that session, but the two have started drifting in different directions (this doc has deeper Module F/architecture detail; that one had 2 days of real findings — the Schedule II mismatch, the hotspot provenance, the composition-check bug fix — that hadn't made it back here until this run).

## 2026-07-10 (eighth run)

- No changes since last sync — both repos' HEAD are identical to the last-synced hashes.
- Backend HEAD unchanged: `4a763d1998b7a5f023cdcc0a3013b5cca5bf5ee2`
- Frontend HEAD unchanged: `1c6f04a129d80c0ac6cae3f8b3d986a88adcadaf`
- No commits to review; PRODUCT_DOCUMENT.md not touched.
- Note (observation only, not a doc change): backend working tree still carries the prior run's uncommitted PRODUCT_DOCUMENT.md edit plus uncommitted changes to `SecurityConfig.kt`, `BatteryCategory.kt`, `RecoveryTargets.kt` (the Schedule II mismatch flagged §6/§8 last run may be getting worked on here), `EvidenceController.kt`, `RecyclerController.kt`, `RegulatoryHistoryController.kt`, `VerificationCheckController.kt`, auth/check/recycler DTOs, `Producer`/`Recycler`/`VerificationCheck` entities, `VerificationCheckRepository.kt`, `VerificationCheckService.kt`, `AuthService.kt`, `PlausibilityCheckService.kt`, `RegulatoryHistoryService.kt`, `application.yml`, gradle wrapper files, `V15__seed_full_cpcb_recycler_directory.sql`, and two test files — none committed, so none reviewed per task scope. Frontend working tree has uncommitted changes to all 3 locale message files only. Both repos also have an untracked `.claude/settings.local.json` (tooling config, not reviewed).

## 2026-07-10 (ninth run — Module B real Schedule II fix, completes what the eighth run observed mid-flight)

- Backend HEAD unchanged: `4a763d1998b7a5f023cdcc0a3013b5cca5bf5ee2` — this run's changes are uncommitted working-tree edits, not a new commit. The eighth run's entry above caught this work in progress (`BatteryCategory.kt`/`RecoveryTargets.kt` mid-edit); this entry documents it complete: `RecoveryTargets.kt`, `BatteryCategory.kt`, `ComplianceCalculatorService.kt`, `ComplianceEstimateRequest.kt`/`ComplianceEstimateResponse.kt`, `EcRates.kt`, `Producer.kt`, `ComplianceCalculatorServiceTest.kt`. No frontend changes this run.
- What changed: the same-day (2026-07-10) stage-1 patch flagged 🟡 in the seventh run's entry is now superseded by a full rebuild, sourced by fetching and reading the actual gazette PDFs directly — S.O. 3984(E) (22-Aug-2022, original Schedule II, all 7 category compliance-cycle tables, 25 pages read in full), S.O. 4669(E) (25-Oct-2023, EV three-wheeler table substitution + one four-wheeler typo fix, full text), G.S.R. 190(E) (14-Mar-2024, carry-forward wording/basis unification, full text), and S.O. 2374(E) (20-Jun-2024, Rule 4(14) recycled-content, full text — needed to resolve an apparent year-axis inconsistency between two secondary sources, which turned out to be a PDF-extraction artifact, not a real discrepancy, once the primary text was read directly).
  - `BatteryCategory` split 4 values → 7 (`PORTABLE_RECHARGEABLE`/`PORTABLE_NON_RECHARGEABLE`, `AUTOMOTIVE`, `INDUSTRIAL`, `EV_THREE_WHEELER`/`EV_TWO_WHEELER`/`EV_FOUR_WHEELER`) — Schedule II genuinely tracks these separately (different plateau years, cycle-start years, and `EV_FOUR_WHEELER` alone runs a 14-year not 7-year cycle). Breaking rename of existing enum values — `Producer.batteryCategories`'s raw-string parsing hardened to skip unparseable legacy names rather than throw (that field is otherwise unused by any DTO/service today, low risk but not zero).
  - `RecoveryTargets.kt` rebuilt: real ramp per category (not 3 inferred flat values), each cell citing its Schedule II clause; a `null` cell means the category's cycle hasn't started yet as of that FY (`getRamp` returns `null`, distinct from a 0%) — e.g. `EV_FOUR_WHEELER` has zero applicable cells across every FY this app currently supports (cycle doesn't start until FY2029-30).
  - `ComplianceCalculatorService` returns `applicable: false` + `notApplicableReason` instead of computing a fabricated shortfall when `getRamp` returns null — no `ComplianceEstimate` row persisted for those cases. When applicable, the response also carries `referenceFinancialYear` (which year's placed-quantity Schedule II's % actually applies to — still a same-year-proxy approximation, not real prior-year tracking), `complianceCycleYears` + `recyclingRefurbishmentObligation` (Schedule II's separate always-100%-by-cycle-end obligation), and `carryForwardCapPercent`/`carryForwardBasisNote` (uniform 60%/remaining-quantity-basis post G.S.R. 190(E) — Automotive's old 20% discount is gone).
  - New `RecycledContentMinimums.kt` (Rule 4(14)) — surfaced as an informational `recycledContentObligation` field, explicitly NOT folded into the shortfall/EC-exposure calculation (different quantity base: manufactured dry weight, not collected/placed weight; doesn't apply to any FY this app currently supports anyway). Scoping choice stated explicitly in the file's own header.
  - `EcRates.kt` remapped onto the new 7-category enum (same per-category rate carried to both Portable sub-categories and all 3 EV sub-categories) — the underlying flat-per-category-rate approximation itself (vs. CPCB Guidelines' real per-metal rate) is unchanged, out of scope for this pass.
  - `ComplianceCalculatorServiceTest` fully rewritten and **actually run** (`./gradlew test`, full suite green) — the stage-1 patch's test update had only been hand-traced, not run, due to a tooling issue in that session; this run's suite ran for real, including `compileKotlin`/`compileTestKotlin` first.
  - Also caught in passing while re-reading the primary text: the *original* (pre-stage-1) bug's wrong numbers came from conflating BWMR Rule 10(4)'s "recovery target" table (a RECYCLER-facing material-recovery-efficiency %, used in EPR-certificate generation) with Schedule II's PRODUCER-facing collection target — two different obligations under the same rules with similarly-shaped tables. `RecoveryTargets.kt`'s pre-existing header comment literally cited "Rule 10(4)" while describing Schedule II, which was the tell.
- Changes made to PRODUCT_DOCUMENT.md: §6 Regulatory content — replaced the stale 4-category flat table (which still showed the pre-stage-1 Rule-10(4)-sourced numbers, contradicting the stage-1 prose right below it) with the real 7-category table for the app's 3 supported FYs, plus new paragraphs on the two Schedule II obligations, Rule 4(14), and the reference-year caveat; rewrote the stage-1-patch status line to ✅ with the real-fix writeup and a flagged frontend-breaking-change follow-up. §3 Module B — updated to describe applicability/reference-year/cycle/carry-forward/recycled-content fields, flagged frontend not updated. §8 Known gaps — replaced the stage-1-patch gap bullet with the real remaining gaps (reference-year tracking, Rule 4(14) scoping, EC per-metal-rate approximation, frontend break).
- Not resolved this run, deliberately (stated explicitly rather than left ambiguous, per this task's own instructions): reference-year quantity tracking (needs a producer placed-quantity history store — a distinct, larger product change); Rule 4(14) not folded into the shortfall calc (different obligation, different quantity base); `eprid-frontend` `CalculatorForm`/`lib/api/types.ts` not updated for the new `BatteryCategory` values (frontend repo wasn't in this task's file scope — will break once both repos deploy together, flagged explicitly above and in PRODUCT_DOCUMENT.md).
- Edit is uncommitted in the backend repo working tree, waiting for Eshwar's review.

## 2026-07-14 (tenth run)

- Synced backend HEAD: `fe9d59f7b85e47b1bc05d4298432aae6f06ae5d6`
- Synced frontend HEAD: `cf76a051a451b9eec7ed0ac2738f4e63d5c1ac5e`
- Commits reviewed:
  - Backend: `fe9d59f` — "update battery category handling and compliance calculations for Schedule II adjustments." This is the ninth run's own uncommitted working-tree edit (BatteryCategory 4→7 split, RecoveryTargets rebuild, ComplianceCalculatorService applicability/reference-year/carry-forward/recycled-content fields) landing as a real commit, unchanged from what that run already documented — its diff also carries the ninth run's PRODUCT_DOCUMENT.md/SYNC_LOG.md edits into history. No new doc content needed for the backend side; only status lines needed updating from "uncommitted, awaiting review" to committed (§6 REAL FIX note now dated/committed rather than pending).
  - Frontend: `cf76a05` — "Update BatteryCategory type and enhance compliance estimate response structure." This resolves the frontend gap flagged explicitly in the ninth run's entry and in PRODUCT_DOCUMENT.md §3/§6/§8 ("`eprid-frontend` not updated for the new `BatteryCategory` values, will break"). `lib/api/types.ts` now has the 7-way `BatteryCategory` union + labels and the full nullable-field `ComplianceEstimateResponse` shape (`applicable`/`notApplicableReason`, `referenceFinancialYear`, `complianceCycleYears`, `recyclingRefurbishmentObligation`, `carryForwardCapPercent`/`carryForwardBasisNote`, `recycledContentObligation`). `CalculatorForm.tsx` default category updated to `PORTABLE_RECHARGEABLE`. `CalculatorResult.tsx` now branches on `applicable` (renders an info card + optional recycled-content section instead of a shortfall when a category's collection cycle hasn't started) and adds a `RecycledContentSection` component for the Rule 4(14) ramp display.
- Changes made to PRODUCT_DOCUMENT.md: §3 Module B — frontend bullet updated from "not updated this pass, will break" to describe what `cf76a05` actually built. §6 — REAL FIX paragraph's status line changed from "uncommitted, awaiting review" to "Committed 2026-07-13 (`fe9d59f`)"; the frontend follow-up paragraph rewritten from a forward-looking warning to a resolved note. §8 — Module B gap bullet's frontend clause updated from "not yet updated... will break" to "resolved 2026-07-13."
- Working-tree check: backend `git status` still shows the same long-standing set of modified files (SecurityConfig, Evidence/Recycler/RegulatoryHistory/VerificationCheck controllers, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, application.yml, V15 migration, PlausibilityCheckServiceTest) plus PRODUCT_DOCUMENT_SYNC_LOG.md (this run's own prior-state) and an untracked `.claude/settings.local.json`. Re-verified with `git diff --ignore-all-space --stat`: zero non-whitespace lines outside PRODUCT_DOCUMENT_SYNC_LOG.md — confirms the eighth/ninth run's finding that this set is CRLF/LF noise, not functional drift; still safe to not flag as a review risk. Frontend working tree shows `messages/en.json`/`hi.json`/`te.json` modified — checked with `git diff --ignore-all-space --ignore-blank-lines`: zero real content lines changed (pure re-indentation), not new translation keys for the 7-way category labels. No untracked frontend files besides `.claude/settings.local.json`.
- Not documented (no committed support / out of scope): the recycled-content translation strings for the new `BatteryCategory` labels don't appear to exist in any locale file yet (en/hi/te all still whitespace-only diffs) — if the calculator UI is meant to be localized, translation keys for the 7 new category labels and the `RecycledContentSection` copy haven't been added. Flagging as an observation only, not a §8 gap, since it's not clear this was ever in scope for i18n.
- Edit is uncommitted in the backend repo working tree (PRODUCT_DOCUMENT.md + this log), waiting for Eshwar's review.

## 2026-07-15 (eleventh run)

- Synced backend HEAD: `199a39ec25d95213f27a8c5aa0c06cb7eee4d102`
- Synced frontend HEAD: `a17945476dc9662cc603548b52bedbd1725156a0`
- Commits reviewed:
  - Backend: `199a39e` — "implement CPCB recycler directory refresh functionality with new repositories and JSON fetching." This commit's own diff also carried the tenth run's uncommitted PRODUCT_DOCUMENT.md/SYNC_LOG.md edits into history (already accurate, no correction needed). New code: `CpcbRecyclerJsonFetcher` pulls the live CPCB `recyclerview` dataset directly (reverse-engineered Laravel DataTables AJAX contract — `draw`/`start`/`length=-1` params + `X-Requested-With` header returns full JSON, 555/555 rows in one call, no pagination); `CpcbRecyclerRefreshService` runs it nightly (`@Scheduled(cron = "0 30 4 * * *")`) or on demand (`POST .../refresh`, `ADMIN`), diffs 9 scoring-relevant tracked fields per recycler against the prior stored row (`CpcbRecyclerSnapshotDiff`, one row per changed field, not per unchanged recycler — uses `BigDecimal.compareTo` not `equals()` for `recycling_capacity` specifically to avoid a scale-mismatch false-positive on every row), rescoes changed/new rows, and gates a risk-**band** flip (not a sub-score-number move) behind a new `pendingReview` flag — `CpcbRecyclerSearchService` holds back the just-flipped score from public search (shows the prior score/band) until `POST .../{id}/confirm-review`. A previously-seen `cpcbId` missing from a pull is flagged `noLongerListedAt`, never deleted. Row-mapping/data-quality-heuristic logic (partial-capture blank-count, India geo bounding box, known-test-row detection) was extracted out of `CpcbRecyclerIngestionService` into a new shared `CpcbRecyclerRowMapper` so the manual-CSV and live-refresh paths can't fork. New `CpcbRefreshRun` entity/table (one row per run, fetched/changed/new/missing counts + status) and `CpcbRecyclerSnapshotDiff` entity/table; migration V21 adds both tables plus 3 new `CpcbRecycler` columns (`last_synced_at`, `pending_review`, `no_longer_listed_at`). New repository methods: `findAllByPendingReviewTrue`, `findAllByCpcbIdIsNotNull`, `findTop2ByRecyclerIdOrderByScoredAtDesc` (the "top 2" is deliberate — index 1 is the pre-flip score to keep showing while index 0's flip sits in review).
  - Frontend: `a179454` — "Add CPCB Review page and admin navigation link; enhance API for refresh and pending review." New `/admin/cpcb-review` page (client-side `ADMIN`-role gate, not server-enforced) with a manual "refresh now" button, recent refresh-run history table (status-colored), and the pending-review queue (each item's latest score + up to 20 recent diffs, field names mapped through a local `FIELD_LABELS` dict to human labels). New `Navbar` link ("CPCB Review") shown only for `ADMIN`. `lib/api/cpcbRecyclers.ts` gained `triggerCpcbRefresh`/`listCpcbRefreshRuns`/`listCpcbPendingReview`/`confirmCpcbReview`; `lib/api/types.ts` gained the matching DTOs plus a `lastSyncedAt` field on `CpcbRecyclerSearchResult` (not yet rendered as a "data as of" label on the public `/cpcb-directory` search cards — only consumed on the new admin page).
- Changes made to PRODUCT_DOCUMENT.md: §3 Module F — added a new paragraph describing the refresh pipeline (fetcher contract/verification claim, diff-and-flag mechanics, band-flip review gate, missing-row flagging, `lastSyncedAt`), expanded the backend bullet with the new classes/migration V21/4 new routes, expanded the frontend bullet with the new `/admin/cpcb-review` page and Navbar link. §4 Data model — added `CpcbRefreshRun`/`CpcbRecyclerSnapshotDiff` rows and a new paragraph on `CpcbRecycler`'s 3 new fields. §8 Known gaps — added a new Module F refresh-pipeline bullet: the fetcher's own class-doc ("verified live 2026-07-14") contradicts `application.yml`'s comment on the *same commit* ("not independently verified this session") — flagged as an unresolved internal inconsistency, not resolved as fact either way; no failure-notification path beyond manually checking `/refresh-runs`; no SLA/badge/count on the pending-review queue outside the dedicated admin page; two upsert paths (CSV + live) share mapping code but have no cross-reconciliation if run close together.
- Not documented (observation only): frontend's new `lastSyncedAt` field on `CpcbRecyclerSearchResult` is plumbed through the API layer but the public `/cpcb-directory` search page/`CpcbRecyclerResultCard` doesn't appear to render it yet — only the new admin page's data implicitly reflects recency via refresh-run timestamps. Not counted as a gap since it may simply not have been in this commit's scope.
- Working-tree check: backend `git status` shows the same long-standing modified-file set as prior runs (SecurityConfig, Evidence/Recycler/RegulatoryHistory/VerificationCheck controllers, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, application.yml, V15 migration, PlausibilityCheckServiceTest) — previously confirmed CRLF/LF noise, not re-verified again this run but no new files appeared in that set. Frontend working tree clean apart from the usual untracked `.claude/settings.local.json`.
- Edit is uncommitted in the backend repo working tree (PRODUCT_DOCUMENT.md + this log), waiting for Eshwar's review.

## 2026-07-17 (twelfth run)

- Synced backend HEAD: `b402d6eb5a0dee7484cf3a8e20c4690a924fe25c`
- Synced frontend HEAD: `0a5ff25b51104a7b9d2859435f76fb2272503783`
- Commits reviewed:
  - Backend: `4ee01c8` (2026-07-15) — "enhance KYC verification by adding PAN validation and update configuration for database connection." This commit's own diff also carried the eleventh run's uncommitted PRODUCT_DOCUMENT.md/SYNC_LOG.md edits into history (already accurate, no correction needed). Actual new code: `CredentialCheckType` gains `PAN_VERIFICATION`; `RegisterRequest` gains `pan`; `AuthService.register` calls `kycProvider.verifyPan(pan, legalName)` only when *both* fields are present (name-match check, not PAN-format-only); `KycProvider`/`StubKycProvider` gain the method (stub still returns `COULD_NOT_VERIFY`, no real vendor). Also in this commit, unrelated to KYC: `VaultDocType.EPR_REGISTRATION_CERT` removed (with an in-code comment citing CPCB's own FAQ — no separate "EPR registration certificate" exists; registration is only via Form 1(B)/Form 2(B), already represented by `REGISTRATION_CERT`) and its `VaultService` display-info row removed to match — not documented in PRODUCT_DOCUMENT.md since Module C1's existing text was already generic ("registration cert, GST cert...") and never named the removed enum value, so nothing was actually stale. Also: `build.gradle.kts`/`application.yml` DB credentials no longer default to `eprid`/`eprid` (now `error()`/required env vars), and `docker-compose.yml` deleted — pure local-dev/deployment config, no product-facing behavior, not documented (doc has no local-dev-setup section for it to go stale in). Also in this commit: `ReportService` PDF disclaimer expansion — findings now print a `Dated:` field, a one-line disclaimer note added directly under the overall rating, and the closing disclaimer paragraph gained three new sentences (findings not independently re-verified with the recycler, recycler hasn't had a chance to respond, source data may have changed since).
  - Frontend: `0a5ff25` (2026-07-15) — "Add disclaimer to CompositeScoreCard and translations in English, Hindi, and Telugu." Direct frontend counterpart to the `ReportService` disclaimer work above: `CompositeScoreCard` now renders the same disclaimer text inline (not just in the downloaded PDF), new `disclaimer` key added to `messages/en.json`/`hi.json`/`te.json` (hi/te currently hold the English string, not yet translated — noted below, not treated as a doc-worthy gap since it matches this repo's existing pattern of landing English strings in all three locale files first).
  - Backend: `b402d6e` (2026-07-16) — "implement row-level transaction management for CPCB recycler refresh to handle individual row errors." `CpcbRecyclerRefreshService` now wraps each row's DB write in its own `TransactionTemplate` (`PROPAGATION_REQUIRES_NEW`) instead of relying on the outer `@Transactional` run-level transaction — a single bad row (constraint violation, malformed data) rolls back only that row and lands in the run's per-row error list, instead of marking the whole run's transaction rollback-only and cascade-failing every subsequent row. Pure reliability fix to logic already documented in §3 Module F; no new entities/routes/migrations.
- Changes made to PRODUCT_DOCUMENT.md: §3 Module A0 — added PAN to the supplied-fields list and the credential-check list, noted the PAN check's dual-field (pan + legalName) gating, updated the stub-provider sentence to say "every check type, PAN included." §4 Data model — `RecyclerCredentialCheck` row updated to `(GST/Udyam/MCA/PAN)`. §3 Module A — report-generator paragraph expanded with the `Dated:` field and the new/expanded disclaimer copy, cross-referenced to the frontend; frontend bullet's `CompositeScoreCard` description updated to mention the new inline disclaimer note. §3 Module F — refresh-pipeline paragraph gained a sentence on the new per-row `REQUIRES_NEW` transaction isolation.
- Not documented (observations only, not gaps): (1) `hi.json`/`te.json`'s new `disclaimer` string is English, not translated — consistent with how this repo has landed new copy in the past (translated in a later pass), not flagged as a product gap. (2) `VaultDocType.EPR_REGISTRATION_CERT` removal and the DB-credentials/docker-compose config change are real commits but don't correspond to anything currently claimed in PRODUCT_DOCUMENT.md, so there was nothing stale to fix — noted here for traceability only.
- Working-tree check: backend `git status` still shows the same long-standing modified-file set as prior runs (SecurityConfig, Evidence/Recycler/RegulatoryHistory/VerificationCheck controllers, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, application.yml, V15 migration, PlausibilityCheckServiceTest) — previously confirmed CRLF/LF noise, not re-verified again this run. New this run: two untracked files, `gradlew_nocrlf.sh` and `.claude/settings.local.json` (tooling, not reviewed). Frontend working tree shows `messages/en.json`/`hi.json`/`te.json` modified plus an untracked `.claude/settings.local.json` — not diffed this run, likely the same re-indentation pattern seen in run ten but not confirmed.
- Edit is uncommitted in the backend repo working tree (PRODUCT_DOCUMENT.md + this log), waiting for Eshwar's review.

## 2026-07-19 (fourteenth run)

- No changes since last sync — both repos' HEAD are identical to the last-synced hashes.
- Backend HEAD unchanged: `66138ad4b603874cc18464f50b84be549f70db5c`
- Frontend HEAD unchanged: `072538f6f8d48903782185579fe079c81ecb0817`
- No commits to review; PRODUCT_DOCUMENT.md not touched.

## 2026-07-17 (thirteenth run)

- Synced backend HEAD: `66138ad4b603874cc18464f50b84be549f70db5c`
- Synced frontend HEAD: `072538f6f8d48903782185579fe079c81ecb0817`
- Commits reviewed:
  - Backend: `66138ad` (2026-07-17) — "enhance logging and security features with request correlation ID and user context." This commit's own diff also carried the twelfth run's uncommitted PRODUCT_DOCUMENT.md/SYNC_LOG.md edits into history (already accurate, no correction needed). Actual new code, three distinct pieces: (1) new `RequestLoggingFilter` (assigns/reuses `X-Request-Id`, MDC `requestId`/`method`/`path`, one summary log line per request) wired into `SecurityConfig` ahead of `JwtAuthFilter`, `JwtAuthFilter` itself now also puts `userId` in MDC, new `logback-spring.xml` (JSON logs via Logstash encoder under the `prod` profile, human-readable elsewhere, both including the MDC fields) replacing the plain `application.yml` console pattern. (2) `FileStorageService.store` fix: extension is now alphanumeric-filtered before use, and the resolved target path is normalized + checked to stay inside the per-check directory — closes a path-traversal opening via a crafted `originalFilename`. (3) `VaultService.listForRecycler` fix: previously returned the full doc list to any authenticated caller who knew the `recyclerId`; now scoped to the owning recycler, or (for everyone else) only the caller's own uploads — closes a cross-tenant IDOR. None of these three were flagged as a §8 gap beforehand (they were undocumented latent issues, not known/tracked ones) — verified by grep against the pre-edit doc text before writing this entry.
  - Frontend: `072538f` (2026-07-17) — "Enhance error handling in apiFetch with ApiError class and requestId logging." Direct counterpart to the backend's request-correlation work: new exported `ApiError` class (`status`, optional `requestId`) thrown instead of a plain `Error` on failed requests; `apiFetch` now reads `X-Request-Id` off the response and logs the failure (path + message + requestId) to the browser console before throwing.
- Changes made to PRODUCT_DOCUMENT.md: §2 Architecture — new paragraph on the request-correlation/logging pipeline (backend `RequestLoggingFilter`/`logback-spring.xml`, frontend `ApiError`/requestId console logging), covering both repos' halves of `66138ad`/`072538f` together since neither commit makes sense read alone. §3 Module A — new paragraph ahead of the backend-routes bullet documenting the `FileStorageService` path-traversal fix. §3 Module C1 — extended the `VaultController` bullet with the `listForRecycler` IDOR fix (the line already said "ownership-checked download," which was accurate only for the download endpoint, not the list endpoint this fixes).
- Not documented (deliberately, per task scope — uncommitted): frontend working tree has a new untracked `components/analytics/` directory plus modified `app/[locale]/layout.tsx` and all 3 locale message files — none committed, so not reviewed or described. Backend working tree unchanged from the long-standing CRLF-noise set flagged in prior runs (SecurityConfig, Evidence/Recycler/RegulatoryHistory/VerificationCheck controllers, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, application.yml, V15 migration, PlausibilityCheckServiceTest), plus the same two untracked tooling files (`gradlew_nocrlf.sh`, `.claude/settings.local.json`) noted last run — not re-verified this run.
- Edit is uncommitted in the backend repo working tree (PRODUCT_DOCUMENT.md + this log), waiting for Eshwar's review.

## 2026-07-19 (fifteenth run)

- No changes since last sync — both repos' HEAD are identical to the last-synced hashes.
- Backend HEAD unchanged: `66138ad4b603874cc18464f50b84be549f70db5c`
- Frontend HEAD unchanged: `072538f6f8d48903782185579fe079c81ecb0817`
- No commits to review; PRODUCT_DOCUMENT.md not touched.

## 2026-07-20 (sixteenth run)

- No changes since last sync — both repos' HEAD are identical to the last-synced hashes.
- Backend HEAD unchanged: `66138ad4b603874cc18464f50b84be549f70db5c`
- Frontend HEAD unchanged: `072538f6f8d48903782185579fe079c81ecb0817`
- No commits to review; PRODUCT_DOCUMENT.md not touched.
- Note (observation only, not a doc change): backend working tree now additionally shows `CpcbRecyclerScoring.kt` and `CpcbRecyclerScoringTest.kt` as modified — new to this run's uncommitted set, not part of the previously-confirmed CRLF-noise files. Not diffed for content this run (out of scope while uncommitted); flag for a content check (real change vs. line-ending noise) once it lands in a commit. Rest of the long-standing uncommitted backend set (SecurityConfig, Evidence/Recycler/RegulatoryHistory/VerificationCheck controllers, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, application.yml, V15 migration, PlausibilityCheckServiceTest) unchanged, still uncommitted. Frontend working tree has uncommitted changes to `app/[locale]/layout.tsx`, `CpcbRecyclerResultCard.tsx`, and all 3 locale files, plus a new untracked `components/analytics/` directory (first seen last run) — none committed, not reviewed.

## 2026-07-21 (seventeenth run)

- Synced backend HEAD: `99fe90437c6649372029633a821cc22b61e59090`
- Synced frontend HEAD: `d213ea8d0805f39a899f475c6b9eb32c8a03fd47`
- Commits reviewed:
  - Backend: `99fe904` (2026-07-21) — "update consent and authorization expiration messages for clarity and provenance." Titled around "authorization" but the only files touched are `CpcbRecyclerScoring.kt`/`CpcbRecyclerScoringTest.kt` — this is the content behind the working-tree changes flagged unreviewed in the sixteenth run's entry, now landed as a real commit. Expired-consent and expired-HWMD flags now name the specific expired date(s) instead of a generic "expired" statement, and both append a provenance clause citing a `dataAsOf` date resolved through `sourceUpdatedAt` → `lastSyncedAt` → `ingestedAt` (first non-null) plus a fixed "not independently confirmed against the state Pollution Control Board" caveat. Checked: all three source fields (`sourceUpdatedAt`, `lastSyncedAt`, `ingestedAt`) already existed on `CpcbRecycler` from earlier work — this commit only changes what the flag text surfaces, not the data model. Tests updated to match the new flag wording.
  - Frontend: `d213ea8` (2026-07-21) — "Integrate Google Analytics component and add roadmap section with translations." Three distinct pieces: (1) new `GoogleAnalytics` component (GA4, production-only + measurement-ID-gated, `anonymize_ip: true`), mounted once in the locale root layout. (2) New public "Now/Next" roadmap section on the landing page (`NowNextSection`, anchored `#now-and-next`), linked from both nav bars, the footer, and the who-it's-for section's "Verified Recycler" badge (now a link, not a static span, copy changed to "— Coming Soon"); content sourced from new `landing.nowNext` translation keys in all 3 locale files. (3) `CpcbRecyclerResultCard` now renders a "data as of" line from `result.lastSyncedAt` when present — resolves the frontend-side observation flagged (not as a doc gap) back in the eleventh run, where `lastSyncedAt` was plumbed through the API layer but only shown on the admin review page. Also: hero/problem-bar fraud stat corrected from "₹700Cr+" to "600K–700K" certificates (a rupee figure was never sourced; the certificate-count framing matches this project's own stated numbers).
- Changes made to PRODUCT_DOCUMENT.md: §3 Landing page — added the Now/Next roadmap section, the Google Analytics integration, and the corrected fraud stat; frontend bullet expanded with the two new components. §3 Module F — new "Flag provenance" paragraph on the `CpcbRecyclerScoring` change; frontend bullet updated to note the CPCB-directory search card's new "data as of" line and that it resolves the eleventh-run observation.
- Not documented (deliberately, out of scope while uncommitted): backend working tree still carries the same long-standing uncommitted set (SecurityConfig, Evidence/Recycler/RegulatoryHistory/VerificationCheck controllers, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, application.yml, V15 migration, PlausibilityCheckServiceTest) plus this task's own PRODUCT_DOCUMENT.md/SYNC_LOG.md edits from the sixteenth run landing in `99fe904`'s diff (already accurate, no correction needed) — not re-verified as CRLF-noise-only this run. Frontend working tree not re-checked this run.
- Edit is uncommitted in the backend repo working tree (PRODUCT_DOCUMENT.md + this log), waiting for Eshwar's review.

## 2026-07-23 (nineteenth run)

- No changes since last sync — both repos' HEAD are identical to the last-synced hashes.
- Backend HEAD unchanged: `35aebfc7c9309634fb058850deb07c0c8886134e`
- Frontend HEAD unchanged: `d213ea8d0805f39a899f475c6b9eb32c8a03fd47`
- No commits to review; PRODUCT_DOCUMENT.md not touched.
- Note (observation only, not a doc change): backend working tree unchanged from the long-standing uncommitted set flagged in prior runs (SecurityConfig, CpcbRecyclerController, Evidence/Recycler/RegulatoryHistory/VerificationCheck controllers, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, VerificationCheckRepository, VerificationCheckService, AuthService, PlausibilityCheckService, RegulatoryHistoryService, ReportService, gradle wrapper files, application.yml, V15 migration, PlausibilityCheckServiceTest), plus this task's own PRODUCT_DOCUMENT.md/SYNC_LOG.md edits and the untracked `.claude/settings.local.json` — still uncommitted, not re-verified as CRLF-noise-only this run given the set has grown since the eighteenth run's snapshot (RegulatoryHistoryService, ReportService, AuthService, VerificationCheckRepository are new to the uncommitted list). Frontend working tree has uncommitted changes to `RiskPill.tsx` and all 3 locale files, plus untracked `.claude/settings.local.json` — not reviewed (uncommitted, and zero frontend commits since last sync).
- The still-unresolved production persistence incident and live unauthenticated `/auth/_diag` endpoint flagged in the eighteenth run's §8 addition have not been addressed by any commit — remains open, no doc change needed since it's already documented.

## 2026-07-22 (eighteenth run)

- Synced backend HEAD: `35aebfc7c9309634fb058850deb07c0c8886134e`
- Synced frontend HEAD: `d213ea8d0805f39a899f475c6b9eb32c8a03fd47` (unchanged — no frontend commits since seventeenth run)
- Commits reviewed (backend, 7 since last run):
  - `99fab5e` (2026-07-21) — "add RecyclerLinkSuggestion entity and repository for CPCB directory link suggestions." Its own diff also carried the seventeenth run's uncommitted PRODUCT_DOCUMENT.md/SYNC_LOG.md edits into history (already accurate, no correction needed). Real product feature, two parts: (1) **capacity-source-aware plausibility.** New `CapacitySource` enum (`CPCB_VERIFIED`/`SELF_REPORTED`), `resolveEffectiveCapacity` in `GenericPlausibilityChecks.kt` prefers the linked `CpcbRecycler.recyclingCapacity` when present and >0, else falls back to the recycler's self-reported figure (labeled as such in the check's own text either way); `PlausibilityCheck.capacitySource` (`V22` migration) persists which was used. `CompositeScoringService`'s hard-disqualification rule 1 (batch >3x capacity) now additionally requires `capacitySource == CPCB_VERIFIED` before it fires — a self-reported number alone can no longer hard-disqualify. (2) **Recycler↔CPCB-directory linking gets an admin surface.** New `RecyclerLinkSuggestion` entity/table (`match_tier` GST_EXACT/NAME_FALLBACK, `status` PENDING/ACCEPTED/REJECTED, unique-pending-pair index) plus a new `ADMIN`-only `RecyclerLinkSuggestionController` (list pending / accept / reject / one-time backfill). `CpcbRecyclerLinkService` gained `generateSuggestions` (dedupes against existing PENDING rows) and `resolveSuggestion`; `VerificationCheckService.createCheck` now calls `generateSuggestions` right after each check's `Recycler` row is upserted, so candidates surface automatically going forward — still never auto-links on any tier, including exact-GST, per the service's existing documented rationale (GST-format normalization between `recyclers.gst_number` and `cpcb_recyclers.recycler_gst_no` was never confirmed against real data). No frontend review UI for the new controller — checked, `eprid-frontend` has zero commits this run, confirmed nothing consumes these routes yet.
  - `8afab00` (2026-07-21) — "modify Flyway configuration to avoid fail-fast on missing environment variables during Docker builds" (`build.gradle.kts` only — deployment/build config, not documented, doc has no build-pipeline section for it to go stale in).
  - `fe9951b`, `3354bd7`, `1b617d9`, `4a54a90`, `35aebfc` (all 2026-07-21/22, same evening) — a sequence of temporary diagnostic commits, all touching only `AuthController.kt`, all building toward one investigation. Read each diff and the final state of the file (not just commit messages, several of which undersell what got added): they progressively build `GET /api/v1/auth/_diag`, an **unauthenticated** endpoint (falls under the existing `/auth/**` public rule, no `@PreAuthorize` added) investigating a genuine production anomaly — the commit authors' own commit messages describe app-side inserts into `eprid.users` being invisible to an external psql/psycopg2 client on the same `DB_URL`, and Flyway's boot log connecting to a real Postgres 16.14/Neon-shaped server while the live `DataSource` bean reports Railway's internal Postgres, within the same running instance. The endpoint's final state (confirmed by reading the current file, not just the diffs) returns: DB connection metadata (`current_database()`, `inet_server_addr()`, backend PID, PG version), live user count + the 5 most recent users' id/email/createdAt, every env var key matching `DATASOURCE`/`DB_`/`DATABASE_URL`/`FLYWAY`/`NEON` (password-like keys redacted, others not), the full sorted list of every env var key name on the instance, `Environment.getProperty` reads for `spring.datasource.url`/`spring.flyway.url`/`spring.flyway.user`, and Railway deployment/replica/snapshot IDs + JVM start time. This is a real, currently-live, unauthenticated PII/infra-topology exposure, not a hypothetical — flagged as a new urgent §8 bullet rather than described as a normal feature; per this task's rules, I did not attempt to fix or remove it myself (out of scope — this task documents code, it doesn't modify it), and did not speculate on whether the underlying persistence bug is real vs. a red herring, only reported what the commits and endpoint actually do.
- Changes made to PRODUCT_DOCUMENT.md:
  - §3 Module A — plausibility-check bullet updated to describe CPCB-verified-vs-self-reported capacity resolution; hard-disqualification paragraph rewritten (rule 1 now gated on `capacitySource`, linking now has an admin REST surface with no frontend UI yet, backfill/auto-generation-at-check-creation described); backend-routes bullet gained the 4 new `/admin/recycler-link-suggestions` routes.
  - §4 Data model — added `RecyclerLinkSuggestion` row; added a paragraph noting `PlausibilityCheck.capacitySource`.
  - §8 Known gaps — added a new 🔴 first bullet on the unresolved production persistence incident and the live unauthenticated `/auth/_diag` endpoint; rewrote the hard-disqualification bullet to reflect rule 1's new CPCB_VERIFIED gate and the new (frontend-less) admin linking surface.
- Not documented (deliberately, out of scope while uncommitted): backend working tree still carries the same long-standing uncommitted set flagged in prior runs (SecurityConfig, Evidence/Recycler/RegulatoryHistory/VerificationCheck controllers, auth/check/recycler DTOs, Producer/Recycler/VerificationCheck entities, gradle wrapper files, application.yml, V15 migration, PlausibilityCheckServiceTest) plus this run's own PRODUCT_DOCUMENT.md/SYNC_LOG.md edits — not re-verified as CRLF-noise-only this run. Frontend not checked for working-tree state this run (zero commits, and reviewing uncommitted frontend changes wasn't necessary to document anything committed).
- Edit is uncommitted in the backend repo working tree (PRODUCT_DOCUMENT.md + this log), waiting for Eshwar's review.
